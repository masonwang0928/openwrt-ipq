name: Build RE-CS-07 (IPQ60xx + NSS + ECM + FW4 + 全套插件)

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update Server
        run: |
          sudo apt update -y
          sudo apt upgrade -y

      - name: Install Dependencies
        run: |
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev python3 python3-pip python3-setuptools python3-wheel python3-pyelftools zlib1g-dev libbz2-dev zip unzip reconf wget curl rsync ccache device-tree-compiler quilt squashfs-tools

      - name: Clone Source (QSDK NSS IPQ60xx)
        run: |
          git clone https://github.com/qosmio/openwrt-ipq.git -b main-nss openwrt
          cd openwrt

      - name: Write .config
        run: |
          cd openwrt
          cat > .config << 'EOF'
# ==============================================
#  RE-CS-07 专属 | IPQ60xx | NSS+ECM | 纯有线
#  锁定：FW4(nftables) 新版防火墙 | 无IPTABLES
# ==============================================
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq60xx=y
CONFIG_TARGET_qualcommax_ipq60xx_DEVICE_jdcloud_re-cs-07=y
CONFIG_TARGET_BOARD="qualcommax"
CONFIG_TARGET_SUBTARGET="ipq60xx"
CONFIG_TARGET_PROFILE="DEVICE_jdcloud_re-cs-07"
CONFIG_TARGET_ARCH_PACKAGES="armcortex-a53"

# ================================
#  NSS + ECM 硬件加速
# ================================
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_kmod-qca-nss-drv-pppoe=y
CONFIG_PACKAGE_kmod-qca-nss-drv-qdisc=y
CONFIG_PACKAGE_qca-nss-firmware-ipq60xx=y

CONFIG_NSS_DRV_IPV4_ENABLE=y
CONFIG_NSS_DRV_PPPOE_ENABLE=y
CONFIG_NSS_DRV_NAT_ENABLE=y
CONFIG_NSS_DRV_FAST_NAT_ENABLE=y
CONFIG_ECM_FRONT_END_SELECTION=0
CONFIG_ECM_CLASSIFIER_DEFAULT=1

# ================================
#  系统性能优化
# ================================
CONFIG_CPU_FREQ_GOV_PERFORMANCE=y
CONFIG_CCACHE=y
CONFIG_FASTNAT=y
CONFIG_RPS=y
CONFIG_RFS=y

# ================================
#  彻底关闭无线
# ================================
CONFIG_WIRELESS=n
CONFIG_PACKAGE_kmod-ath11k=n
CONFIG_PACKAGE_hostapd=n
CONFIG_PACKAGE_wpa-supplicant=n

# ================================
#  锁定 新版防火墙 FW4 (NFTABLES)
#  彻底禁用老版 iptables/fw3
# ================================
CONFIG_PACKAGE_firewall4=y
CONFIG_PACKAGE_firewall3=n
CONFIG_PACKAGE_iptables=n
CONFIG_PACKAGE_iptables-nft=n
CONFIG_PACKAGE_ip6tables=n
CONFIG_PACKAGE_kmod-ipt-nat=n
CONFIG_PACKAGE_kmod-ipt-nathelper=n
CONFIG_PACKAGE_kmod-ipt-filter=n

# ================================
#  双WAN + MWAN3 负载均衡
# ================================
CONFIG_PACKAGE_mwan3=y
CONFIG_PACKAGE_luci-app-mwan3=y
CONFIG_PACKAGE_ppp=y
CONFIG_PACKAGE_pppoe=y
CONFIG_PACKAGE_iproute2=y
CONFIG_PACKAGE_ipset=y

# ================================
#  LuCI 中文界面 + 3套主题
# ================================
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-app-firewall=y

CONFIG_PACKAGE_luci-theme-argon=y
CONFIG_PACKAGE_luci-theme-edge=y
CONFIG_PACKAGE_luci-theme-opentopd=y

# ================================
#  CPU 性能调节 & 状态监控
# ================================
CONFIG_PACKAGE_luci-app-cpufreq=y
CONFIG_PACKAGE_luci-app-cpu-status=y
CONFIG_PACKAGE_cpufrequtils=y

# ================================
#  你指定的全部插件
# ================================
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-app-adguardhome=y
CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_luci-app-smartdns=y
CONFIG_PACKAGE_luci-app-aria2=y
CONFIG_PACKAGE_luci-app-vlmcsd=y
CONFIG_PACKAGE_luci-app-nikki=y
CONFIG_PACKAGE_luci-app-mosdns=y
CONFIG_PACKAGE_luci-app-filetransfer=y
CONFIG_PACKAGE_luci-app-mwan3help=y
CONFIG_PACKAGE_luci-app-ntpc=y

# 插件依赖
CONFIG_PACKAGE_adguardhome=y
CONFIG_PACKAGE_samba4-server=y
CONFIG_PACKAGE_smartdns=y
CONFIG_PACKAGE_aria2=y
CONFIG_PACKAGE_vlmcsd=y
CONFIG_PACKAGE_nikki=y
CONFIG_PACKAGE_mosdns=y
CONFIG_PACKAGE_filetransfer=y
CONFIG_PACKAGE_ntpc=y

# ================================
#  IPTV Server (RTP/UDP/RTSP)
# ================================
CONFIG_PACKAGE_luci-app-udpxy=y
CONFIG_PACKAGE_udpxy=y
CONFIG_PACKAGE_luci-app-msd_lite=y
CONFIG_PACKAGE_msd_lite=y
CONFIG_PACKAGE_luci-app-igmpproxy=y
CONFIG_PACKAGE_igmpproxy=y

# ================================
#  关闭无用功能
# ================================
CONFIG_IPV6=n
CONFIG_PACKAGE_docker=n
CONFIG_PACKAGE_istore=n
CONFIG_PACKAGE_nginx=n
EOF

      - name: Update Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Download Dependencies
        run: |
          cd openwrt
          make defconfig
          make download -j$(nproc)

      - name: Build Firmware
        run: |
          cd openwrt
          make -j$(nproc) || make -j1 V=s

      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: RE-CS-07-NSS-ECM-FW4-FULL
          path: openwrt/bin/targets/qualcommax/ipq60xx/*
