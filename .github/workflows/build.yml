name: Test Build Button

on:
  workflow_dispatch:
  push:
    branches: [ main-nss ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Run workflow 按钮已经正常工作了！"
