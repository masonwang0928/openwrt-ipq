name: OpenWrt-IPQ60xx-JDCloud-RECS7-NSS
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 安装依赖
        run: |
          sudo apt update
          sudo apt install -y build-essential flex bison git-core libssl-dev libtool pkg-config libncurses5-dev zlib1g-dev python3 python3-pip unzip wget curl rsync subversion
          sudo -E apt clean

      - name: 拉取源码
        run: |
          git clone https://github.com/qosmio/openwrt-ipq.git -b main-nss openwrt
          cd openwrt
          git checkout main-nss

      - name: 清理旧配置
        run: |
          cd openwrt
          rm -f .config .config.old
          rm -rf dl build_dir staging_dir tmp

      - name: 写入配置
        run: |
          cd openwrt
          cat > .config << 'EOF'
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq60xx=y
CONFIG_TARGET_qualcommax_ipq60xx_DEVICE_jdcloud_re_cs7=y
CONFIG_TARGET_ROOTFS_SQUASHFS=y
CONFIG_TARGET_ROOTFS_PARTSIZE=256
CONFIG_BUILD_PATENTED=y
CONFIG_CCACHE=y
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_nss-firmware-ipq6018=y
CONFIG_PACKAGE_mwan3=y
CONFIG_PACKAGE_luci-app-mwan3=y
CONFIG_PACKAGE_mosdns=y
CONFIG_PACKAGE_luci-app-mosdns=y
CONFIG_PACKAGE_dnsmasq-full=y
CONFIG_PACKAGE_openclash=y
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_iptables-mod-tproxy=y
CONFIG_PACKAGE_kmod-tun=y
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_luci-app-upnp=y
# 禁用无线
# CONFIG_PACKAGE_kmod-ath11k is not set
# CONFIG_PACKAGE_luci-app-wireless is not set
# 禁用冲突包
# CONFIG_PACKAGE_sqm-scripts is not set
# CONFIG_PACKAGE_sqm-scripts-nss is not set
# CONFIG_PACKAGE_iptasn is not set
# CONFIG_PACKAGE_iptables-nft is not set
EOF

      - name: 下载依赖
        run: |
          cd openwrt
          export TERM=xterm
          export FORCE_UNSAFE_CONFIGURE=1
          make download -j8 V=s

      - name: 编译固件
        run: |
          cd openwrt
          make -j1 V=s

      - name: 收集固件
        run: |
          cd openwrt
          mkdir -p ../firmware
          find bin/targets/qualcommax/ipq60xx/ -name "*sysupgrade*.bin" -exec cp {} ../firmware/ \;
          find bin/targets/qualcommax/ipq60xx/ -name "*factory*.bin" -exec cp {} ../firmware/ \;

      - name: 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: JDCLOUD-RECS7-IPQ60XX-NSS-FW
          path: firmware/
